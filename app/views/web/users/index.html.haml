.login
  %h1 Авторизация

  .row
    .col-lg-6
      = simple_form_for '', wrapper: :bootstrap, url: '/login', method: 'post' do |f|
        = f.input :email
        = f.input :password
        = f.button :submit
